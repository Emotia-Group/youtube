# Núcleo
anthropic>=0.116.0        # LLM (concepto, guion, escenas, metadatos, visión)
pyyaml>=6.0
pillow>=10.0              # miniaturas y placeholders

# Lectura de documentos de guion (PDF, Word, PowerPoint, Excel)
pypdf>=4.0
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1

# Proveedores opcionales (instala solo los que uses)
openai>=1.40              # TTS (gpt-4o-mini-tts), STT (whisper-1), imágenes
                          # (GPT Image 2 — gpt-image-1 se apaga el 23/10/2026)
elevenlabs>=1.6           # TTS de alta calidad multilingüe
edge-tts>=6.1             # TTS gratuito (voces neuronales de Edge)
replicate>=0.32           # imágenes (FLUX), video generativo (Kling/Wan/Veo),
                          # ESCALADO del modo híbrido, lipsync, música

# Cartesia (voz ~10x más barata que ElevenLabs) y AssemblyAI (transcripción
# más barata y con mejores tiempos por palabra) NO necesitan paquete: hablan
# por HTTP directo. Basta con definir CARTESIA_API_KEY / ASSEMBLYAI_API_KEY
# en ⚙ Configuración → Claves de API.
yt-dlp>=2024.10           # análisis profundo de videos de referencia por enlace
                          # (guion/transcripción, ritmo de cortes, estilo visual)

# Publicación en YouTube (opcional)
google-api-python-client>=2.100
google-auth-oauthlib>=1.1

# Torre de Control (panel multicanal) — cifra los tokens de tus canales.
# El panel funciona sin este paquete, pero guarda los tokens sin cifrar y avisa.
cryptography>=41
